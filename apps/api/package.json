{
  "name": "@evidex/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch --env-file=../../.env src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun --env-file=.env.test run ../../packages/db/src/resetTest.ts && bun --env-file=.env.test run ../../packages/db/src/migrate.ts && bun --env-file=.env.test test src"
  },
  "dependencies": {
    "@evidex/ai": "workspace:*",
    "@evidex/db": "workspace:*",
    "@evidex/evidence": "workspace:*",
    "@evidex/shared": "workspace:*",
    "drizzle-orm": "^0.45",
    "hono": "^4",
    "pg": "^8",
    "zod": "^4"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1"
  }
}
